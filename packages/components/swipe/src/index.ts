import {defineComponent , h} from 'vue'
import {createNamespace} from "../../../utils/namespace";

const StSwipe = defineComponent({
    name : createNamespace('swipe'),
    props : {

    },
    render() {

    }
})
